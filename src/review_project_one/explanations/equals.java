package review_project_one.explanations;

public class equals {
//    Equals is when we compare two objects to each other and see if they are the same
//    If you do not add this method to a custom class it will 99% of the time return false
//    An example would be the one in Class Hierarchies this is the syntax:
//    @Override <-- override
//    public boolean equals(Object o){ <-- take in the object we are comparing, return bool
//        if (o instanceof Vehicle){ <-- check if object is the same instance
//            FourWheels that = (FourWheels) o; <-- cast object
//            return this.brandName == that.brandName && this.yearMade == that.yearMade; <-- check if they are the same
//        }
//        return false; <-- otherwise false
//    }

}
